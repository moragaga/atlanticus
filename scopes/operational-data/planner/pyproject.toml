[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atlanticus-operational-data-planner"
version = "1.0.0"
description = "Operational data requirement planning for the Atlanticus Operational Data scope."
requires-python = "==3.14.2"
classifiers = ["Private :: Do Not Upload"]
dependencies = ["atlanticus-operational-data-core==1.0.0"]

[dependency-groups]
dev = ["pytest==9.1.1", "ruff==0.15.22"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["atlanticus.operational_data.planner*"]
namespaces = true

[tool.setuptools.package-data]
"atlanticus.operational_data.planner" = ["py.typed"]

[tool.pytest.ini_options]
addopts = "-q --import-mode=importlib"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
line-ending = "lf"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["atlanticus"]
combine-as-imports = true
