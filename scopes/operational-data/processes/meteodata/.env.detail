# Plantilla del proceso Meteodata. Copiar a .env solo en local y reemplazar <...>.
# ENVIRONMENT=local solamente para .env; en despliegue el ambiente es inyectado.
ENVIRONMENT=local

# Abreviaturas de compañía y producto, usadas para identificar Key Vault en despliegue.
COMPANY_ABREV=<company-abrev>
PRODUCT_ABREV=<product-abrev>

# Identidad de la aplicación y ruta ABSOLUTA para datasets, runtime y logs.
APPLICATION=operational-data-meteodata-local
VOLUMEN_PATH=<absolute-local-volume-path>

# API Meteodata; la ruta termina en /met/ y el cliente solicita consultas.
METEODATA_BASE_URL=https://pelambres.meteodata.cl/met/
# Credencial SIN prefijo 'Token'; configurar con secreto en despliegue.
METEODATA_TOKEN=<meteodata-token>

# Parámetros HTTP; todos los tiempos son segundos y deben ser enteros positivos.
METEODATA_CONNECT_TIMEOUT_SECONDS=5
METEODATA_READ_TIMEOUT_SECONDS=15
METEODATA_WRITE_TIMEOUT_SECONDS=15
METEODATA_POOL_TIMEOUT_SECONDS=5
METEODATA_MAX_RESPONSE_BYTES=2097152
METEODATA_VERIFY_TLS=true

# Ventana solapada: consulta los 90 minutos previos, también recupera HM retrasada.
# Ajustar a retraso máximo verificado de HM; no usa watermark global.
METEODATA_LOOKBACK_MINUTES=90
# Solo si no hubo filas nuevas ni corregidas: un reintento interrumpible 60 s después.
# Poner 0 para deshabilitarlo al ejecutar tests manuales.
METEODATA_RETRY_DELAY_SECONDS=60

# El job siempre ejecuta una iteración por proceso; el scheduler externo usa cron */5.

# Observabilidad local y Azure, independientes de la persistencia del dataset.
ATLANTICUS_OBSERVABILITY_FILE_LOGS_ENABLED=true
ATLANTICUS_AZURE_OBSERVABILITY_MODE=off
ATLANTICUS_AZURE_OBSERVABILITY_PROFILE=slim
# Solo para ATLANTICUS_AZURE_OBSERVABILITY_MODE=export; secreto en Key Vault.
APPLICATION_INSIGHTS_CONNECTION_STRING=
