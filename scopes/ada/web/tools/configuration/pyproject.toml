[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ada-web-tools-configuration"
version = "0.1.0"
description = "Configuración administrativa, lifecycle y editor Web de Tools ADA"
requires-python = "==3.14.2"
classifiers = ["Private :: Do Not Upload"]
dependencies = [
    "ada-web-branding==0.1.0",
    "ada-web-tools==0.1.0",
]

[project.optional-dependencies]
web = [
    "atlanticus-web==0.1.0",
    "dash==4.4.1",
]

[tool.uv.sources]
ada-web-branding = { path = "../../branding", editable = true }
ada-web-tools = { path = "../core", editable = true }
atlanticus-web = { path = "../../../../../web/framework/core", editable = true }

[dependency-groups]
dev = [
    "atlanticus-web==0.1.0",
    "dash==4.4.1",
    "pytest==9.1.1",
    "ruff==0.15.22",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
include = ["ada.web.tools.configuration*"]

[tool.setuptools.package-data]
"ada.web.tools.configuration" = ["py.typed"]
"ada.web.tools.configuration.web" = [
    "py.typed",
    "resources/css/*.css",
    "resources/css/css.list",
]

[tool.ruff]
line-length = 100
indent-width = 4
target-version = "py314"
extend-exclude = ["build", "dist", "commented"]

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
line-ending = "auto"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["ada", "atlanticus"]
combine-as-imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
    "src",
    "../core/src",
    "../../branding/src",
    "../../../../../web/framework/core/src",
]
addopts = "-q --import-mode=importlib"
